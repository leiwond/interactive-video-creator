Create interactive Videos by putting questions and answers on top of an existing Video.
Answers can to configured, so they result in a jump to specific video frame.

Run a http-server that supports headers Accept-Ranges: bytes. Otherwise the currentTime of the 
video will be reset to 0 when changed with slider.

Python's http-server doesn't seen to support it yet (16.Mar 2025). 